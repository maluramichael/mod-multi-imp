/*
 * mod-multi-imp — module entry point.
 *
 * The function name must be Addmod_multi_impScripts() so AzerothCore's generated
 * module script loader (derived from the directory name "mod-multi-imp") calls it.
 */

void AddMultiImpScripts();

void Addmod_multi_impScripts()
{
    AddMultiImpScripts();
}
